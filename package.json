{
  "name": "kuaimai-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd server && npm install && npm run build && cp -r dist ../dist && echo 'Build OK, files:' && ls -la ../dist",
    "start": "node dist/main.js"
  }
}
